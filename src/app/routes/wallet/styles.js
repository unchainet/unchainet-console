import colors from "styles/global/colors";

export default (theme) => ({
  unet: {
    extend: colors.unet,
    fontWeight: "bold"
  },
  crc: {
    extend: colors.crc,
    fontWeight: "bold",
  },
  usd: {
    extend: colors.usd,
    fontWeight: "bold"
  }
});
